# 🌊 AlgoFlow

 **AlgoFlow** est un IDE web léger et interactif conçu pour écrire, visualiser et exécuter des algorithmes en direct dans le navigateur.
## 📖 À propos

Conçu pour les étudiants et les passionnés d'algorithmique, **AlgoFlow** simule le comportement d'un langage de programmation tout en utilisant une syntaxe académique française claire.

Il a été développé avec une philosophie simple : **Zéro installation, 100% Web.**

## ✨ Fonctionnalités Principales

* **Exécution Temps Réel** : Interpréteur JavaScript maison qui exécute le code ligne par ligne.
* **Tableaux & Matrices** :
    * Tableaux à 1 dimension (`tab[i]`).
    * Matrices à 2 dimensions (`mat[i,j]`).
    * **Allocation dynamique** : Les tableaux s'agrandissent automatiquement si nécessaire.
* **Structures de Contrôle Complètes** :
    * `Si ... Alors ... Sinon ... FinSi`
    * `Pour ... allant de ... à ... Faire ... FinPour`
    * `TantQue ... Faire ... FinTantQue`
    * `Repeter ... Jusqu'à`
* **Opérateurs** :
    * Arithmétiques : `+`, `-`, `*`, `/`, `Mod` (Modulo).
    * Logiques : `ET`, `OU`, `!` (Non), `<`, `>`, `<=`, `>=`, `=`, `<>` (Différent).
* **Interface Intuitive** :
    * Coloration syntaxique.
    * Console d'entrée/sortie interactive (`Lire` / `Ecrire`).
    * Numérotation des lignes et scroll synchronisé.

## 🛠️ Technologies Utilisées

Ce projet est "Hand-coded" (codé à la main) sans frameworks lourds :
* **HTML5** : Structure sémantique.
* **CSS3** : Design sombre (Dark Mode) inspiré de VS Code.
* **Vanilla JavaScript (ES6+)** : Moteur d'interprétation et logique métier.

## 🚀 Installation et Utilisation

Aucune installation complexe n'est requise (pas de Node.js, pas de Python).

1.  **Cloner le projet** (ou télécharger le ZIP) :
    ```bash
    git clone [https://github.com/votre-nom-utilisateur/AlgoFlow.git](https://github.com/votre-nom-utilisateur/AlgoFlow.git)
    ```
2.  **Lancer** :
    Double-cliquez simplement sur le fichier `index.html` pour l'ouvrir dans votre navigateur web favori.
